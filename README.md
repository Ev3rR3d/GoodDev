# Repositório focado em Maldev

### As ferramentas utilizadas são majoritariamente em rust, para compilar o binário é preciso:

- Para linux
`cargo build --release`

- Para windows
`cargo build --target x86_64-pc-windows-gnu --release`

# *Fazer o teste somente em ambiente controlado*
